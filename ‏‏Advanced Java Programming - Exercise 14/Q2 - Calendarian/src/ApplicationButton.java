/**
 * Enum representing the different application buttons for navigating through the calendar.
 * 
 * The buttons allow the user to navigate between months and years in the calendar view.
 */
public enum ApplicationButton {
	PREVIOUS_MONTH,
	NEXT_MONTH,
	PREVIOUS_YEAR,
	NEXT_YEAR;
}
